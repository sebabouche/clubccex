.row
  .col-sm-2
    .well
      = simple_form_for @model do |f|
        = f.error_notification
        
        = f.input :priority
        = f.input :name

        = f.button :submit, "Créer", class: "btn btn-primary btn-block"

  .col-sm-9
    .row
      - @categories.each do |category|
        .col-sm-3
          %h2
            %span.label.label-info= category.priority
            %span= category.name
