.row
  .col-sm-8.col-sm-offset-2
    = concept('post/cell/show', @post, current_user: tyrant.current_user)

    .divider-30
    %hr

    = concept('comment/cell/form', @form, post: @post, current_user: tyrant.current_user)
    = concept('comment/cell/grid', @post)
